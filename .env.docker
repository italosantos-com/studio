# ============================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ============================================
# Use this file to customize Docker deployment settings
# Copy to .env in the same directory as docker-compose.yml

# ================== DATABASE CONFIGURATION ==================
# PostgreSQL database settings
POSTGRES_USER=postgres
POSTGRES_DB=italosantos
POSTGRES_PORT=5432

# ⚠️ DO NOT SET POSTGRES_PASSWORD HERE
# Password is managed via Docker secrets in secrets/db_password.txt

# ================== APPLICATION CONFIGURATION ==================
# Node.js environment
NODE_ENV=production

# Application port mapping (host:container)
APP_PORT=3000

# ================== NOTES ==================
# 1. Secrets are stored in ./secrets/ directory (added to .gitignore)
# 2. Database password is read from secrets/db_password.txt
# 3. Health checks ensure containers are ready before accepting traffic
# 4. Use docker-compose up -d to start all services
